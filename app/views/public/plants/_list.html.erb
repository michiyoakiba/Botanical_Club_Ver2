<div class="row">  
  <% plants.each do |plant| %>
  <div class="col-3 mx-4">  
    <div class="card mx-4 mt-3 shadow-lg" style="width: 280px;">
      <%= link_to public_plant_path(plant.id) do %>
        <%= image_tag plant.get_image, class:"card-img-top img-fluid" %> 
      <% end %>
      <div class="card-body">
        <div class="row">
          <div class="col-2">
            <%= link_to public_customers_path(plant.customer) do %>
              <%= image_tag plant.customer.get_profile_image(100,100), class:"mt-3 rounded-circle img-fluid" %></p>
            <% end %>
          </div>
          <div class="col-10">
            <p class="card-title">
              <strong><i class="fab fa-envira"></i><span>
                <%= link_to plant.plant_name, public_plant_path(plant), class: "text-dark" %>
              </span></strong>｜ 
            <!--<p class="card-text">説明：<%#= plant.caption %></p>-->
            
            <!--<span> ｜ -->
            <!--  <%#= link_to public_customers_path(plant.customer), class:"text-dark" do %>-->
            <!--    <%#= plant.customer.name %>-->
            <!--  <%# end %>-->
            <!--｜ </span>-->
            
            <!--｜ <span><%#= plant.genre.genre_name %></span>｜ -->
              <span><i class="fas fa-comment"></i>
              <%= link_to plant.plant_comments.count, public_plant_path(plant.id), class: "text-dark" %>｜</span>
            <!--部分テンプレートにした箇所(部分的に更新したい箇所)-->
              <span id="favorite_buttons_<%= plant.id %>">
              <%= render "public/favorites/btn", plant: plant %></span>
              </p>
              <!--% if plant.favorited_by?(current_user) %>-->
              <!--    %= link_to plant_favorites_path(plant), method: :delete do %>-->
              <!--      ♥%= plant.favorites.count %>-->
              <!--    % end %>-->
              <!--% else %>-->
              <!--    %= link_to plant_favorites_path(plant), method: :post do %>-->
              <!--      ♡%= plant.favorites.count %> -->
              <!--    % end %>-->
              <!--% end %>       -->
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
</div>
<div class="d-flex justify-content-center"><%= paginate plants %></div>
